#include "event_loop.h"

// Everything is header-only; this translation unit exists so the build system
// has a stable object to compile (and so future non-template helpers land
// here).
namespace meshcli {
// intentionally empty
}
